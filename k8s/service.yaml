apiVersion: v1
kind: Service
metadata:
  name: svc-mercado-crm
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: mercado-crm
  ports:
    - protocol: TCP
      port: 8102
      targetPort: 8102
      name: http
